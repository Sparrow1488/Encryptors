namespace Encryptors.Enums
{
    public class CryptoResultStateType
    {
        private readonly string _state;

        internal CryptoResultStateType(string state)
        {
            _state = state;
        }
    }
}
